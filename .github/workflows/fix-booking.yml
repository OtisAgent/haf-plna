name: Fix Booking Submission
on:
  workflow_dispatch:

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run patch
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: python3 patch_book.py
